import React from "react";
import { View, Text } from "react-native";

const ProgramDetails = ({ navigation, exersize }) => {
  return (
    <View>
      <Text> {egzersize} </Text>
    </View>
  );
};

export default ProgramDetails;
